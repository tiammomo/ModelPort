[package]
name = "modelport-ops-protocol"
version = "0.1.1"
edition = "2024"
rust-version = "1.96"
license = "MIT"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
